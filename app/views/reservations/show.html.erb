<div class="container chat">
  <h1>Your reservation for <%= @reservation.pet.name %></h1>

  <div id="messages" class="messages">
    <% @reservation.messages.each do |message| %>
      <div id="message-<%= message.id %>">
        <small>
          <strong><%= message.user.nickname %></strong>
          <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
        </small>
        <p><%= message.content %></p>
      </div>
    <% end %>
  </div>
</div>

<%= simple_form_for [@reservation, @message],
  html: {class: "d-flex"} do |f|
%>
  <% other_user = current_user == @reservation.restaurant.user ? @reservation.user : @reservation.restaurant.user %>
  <%= f.input :content,
    label: false,
    placeholder: "Message #{other_user.nickname}",
    wrapper_html: {class: "flex-grow-1"}
  %>
  <%= f.submit "Send", class: "btn btn-primary mb-3" %>
<% end %>