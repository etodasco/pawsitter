<div class="navbar ms-2">
  <h4 id="pawsitter">🐾 Pawsitter 🐾</h4>
    <% if user_signed_in? %>
      <div class="text-end">
        <h5 class="navbar-user me-2" > <i class="fa-regular fa-user"></i> <%= current_user.nickname.to_s.capitalize %></h5>
      </div>
    <% end %>
</div>

<div class="navbar navbar-expand-sm navbar-light navbar-pawsitter">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <!-- Right-aligned welcome message -->
  </div>
</div>


    <!-- Add Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto me-2">
        <% if user_signed_in? %>
        <% else %>
          <li class="nav-item ">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>

<!-- Navbar at bottom -->
  <!-- Navbar Home Button -->
<% if user_signed_in? %>
<nav class="navbar fixed-bottom navbar-light bg-light" style="padding: 0.5rem 0;">
  <ul class="navbar-nav w-100 d-flex flex-row justify-content-around align-items-center" style="padding: 0; margin: 0;">
    <li class="nav-item d-flex flex-column align-items-center">
      <%= link_to root_path, class: 'nav-link text-center' do %>
        <i class="fas fa-home" style="font-size: 1rem;"></i>
        <p class="mb-0" style="font-size: 0.75rem;">Home</p>
      <% end %>
    </li>
    <!-- Navbar Reservations Button -->
    <li class="nav-item d-flex flex-column align-items-center">
      <%= link_to reservations_path, class: 'nav-link text-center' do %>
        <i class="fas fa-calendar-alt" style="font-size: 1rem;"></i>
        <p class="mb-0" style="font-size: 0.75rem;">Reservations</p>
      <% end %>
    </li>
    <!-- Navbar User Button -->
    <li class="nav-item d-flex flex-column align-items-center">
      <%= link_to profile_path, class: 'nav-link text-center' do %>
        <i class="fas fa-user" style="font-size: 1rem;"></i>
        <p class="mb-0" style="font-size: 0.75rem;">User Profile</p>
      <% end %>
    </li>
    <!-- Navbar Log Out Icon -->
    <li class="nav-item d-flex flex-column align-items-center">
      <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link text-center' do %>
        <i class="fas fa-sign-out-alt" style="font-size: 1rem;"></i>
        <p class="mb-0" style="font-size: 0.75rem;">Logout</p>
      <% end %>
    </li>
  </ul>
</nav>

<% end %>